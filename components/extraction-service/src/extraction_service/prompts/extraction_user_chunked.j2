Document part {{ chunk_index + 1 }} of {{ total_chunks }}
Section: {{ section_type }}
{% if page_hint %}Page hint: {{ page_hint }}{% endif %}

Extract inclusion and exclusion criteria from this chunk:

{{ chunk_text }}

Only submit criteria found in this chunk. Use tools to clarify or structure when needed.
