You are analyzing a clinical trial protocol.

For each page below, determine if it contains eligibility criteria
(inclusion or exclusion criteria for patient enrollment).

Return ONLY a JSON object with key "pages" listing page numbers that
contain criteria. Example: {"pages":[1,3,7]}

Pages:
{% for page in pages %}
--- Page {{ page.page_number }} ---
{{ page.text }}
{% endfor %}
